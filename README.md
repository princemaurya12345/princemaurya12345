# Talk with Twerk
A playful Omegle-style video chat UI prototype. Uses browser WebRTC APIs; production random matchmaking needs a signaling/matchmaking service.

## Security
Set secrets only as Vercel Environment Variables. Never expose an OpenAI `sk-proj-*` key in browser code.
